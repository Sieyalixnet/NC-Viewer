pub mod file_manage;

//file_manage is use for opening an NC file and convert them into a JSON struct

pub mod variables;

//variables is a struct that save some variables' attributes like dimensions and name.

pub mod utils;

//some tools to convert the data into String which is for parsing them into JSON